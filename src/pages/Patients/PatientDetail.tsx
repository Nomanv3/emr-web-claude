import PlaceholderPage from '@/components/PlaceholderPage';

export default function PatientDetail() {
  return (
    <PlaceholderPage
      title="Patient Detail"
      description="Full patient profile with demographics, medical history, visits, and invoices will appear here."
    />
  );
}
